# VibePilot Event-Driven Pipeline
# AUTO-GENERATED by scripts/generate_pipeline_yaml.py
# Do not edit manually -- changes will be overwritten on next sync.
# To add events: add router.On() in Go code, then this file updates automatically.

pipeline:
  name: vibepilot-governor
  type: event-driven
  trigger: postgres_notify
  notify_channel: vp_changes
  listener: internal/pgnotify/listener.go

  events:
    - event: council_consensus
      constant: EventCouncilConsensus
      handler: none (no router.On registration)
      handler_file: N/A

    - event: council_review
      constant: EventCouncilReview
      source_table: plans
      handler: handleCouncilReview
      handler_file: cmd/governor/handlers_council.go

    - event: council_started
      constant: EventCouncilStarted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: council_vote
      constant: EventCouncilVote
      handler: none (no router.On registration)
      handler_file: N/A

    - event: courier_result
      constant: EventCourierResult
      source_table: task_runs
      handler: none (no router.On registration)
      handler_file: N/A

    - event: crash_detected
      constant: EventCrashDetected
      handler: none (no router.On registration)
      handler_file: N/A

    - event: improvement_suggested
      constant: EventImprovementSuggested
      handler: none (no router.On registration)
      handler_file: N/A

    - event: maintenance_command
      constant: EventMaintenanceCmd
      source_table: maintenance_commands
      handler: handleMaintenanceCommand
      handler_file: cmd/governor/handlers_maint.go

    - event: model_failure
      constant: EventModelFailure
      handler: none (no router.On registration)
      handler_file: N/A

    - event: model_success
      constant: EventModelSuccess
      handler: none (no router.On registration)
      handler_file: N/A

    - event: prd_ready
      constant: EventPRDReady
      handler: none (no router.On registration)
      handler_file: N/A

    - event: pattern_detected
      constant: EventPatternDetected
      handler: none (no router.On registration)
      handler_file: N/A

    - event: plan_approved
      constant: EventPlanApproved
      source_table: plans
      handler: none (no router.On registration)
      handler_file: N/A

    - event: plan_complete
      constant: EventPlanComplete
      handler: none (no router.On registration)
      handler_file: N/A

    - event: plan_council_review
      constant: EventPlanCouncilReview
      handler: none (no router.On registration)
      handler_file: N/A

    - event: plan_created
      constant: EventPlanCreated
      source_table: plans
      handler: none (no router.On registration)
      handler_file: N/A

    - event: plan_review
      constant: EventPlanReview
      source_table: plans
      handler: none (no router.On registration)
      handler_file: N/A

    - event: plan_review_completed
      constant: EventPlanReviewCompleted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: plan_review_started
      constant: EventPlanReviewStarted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: plan_revision_needed
      constant: EventPlanRevisionNeeded
      handler: none (no router.On registration)
      handler_file: N/A

    - event: recovery_completed
      constant: EventRecoveryCompleted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: recovery_started
      constant: EventRecoveryStarted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: research_report_council_review
      constant: EventReportCouncil
      source_table: research_reports
      handler: handleReportCouncilReview
      handler_file: cmd/governor/handlers_research.go

    - event: research_approved
      constant: EventResearchApproved
      source_table: research_suggestions
      handler: handleResearchApproved
      handler_file: cmd/governor/handlers_research.go

    - event: research_council
      constant: EventResearchCouncil
      source_table: research_suggestions
      handler: handleResearchCouncil
      handler_file: cmd/governor/handlers_research.go

    - event: research_ready
      constant: EventResearchReady
      source_table: research_suggestions
      handler: handleResearchReady
      handler_file: cmd/governor/handlers_research.go

    - event: revision_needed
      constant: EventRevisionNeeded
      source_table: plans
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_approved
      constant: EventTaskApproval
      source_table: tasks
      handler: handleTaskApproved
      handler_file: cmd/governor/handlers_maint.go

    - event: task_available
      constant: EventTaskAvailable
      source_table: tasks
      handler: handleTaskAvailable
      handler_file: cmd/governor/handlers_task.go

    - event: task_checkpoint
      constant: EventTaskCheckpoint
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_claimed
      constant: EventTaskClaimed
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_error
      constant: EventTaskError
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_human_review
      constant: EventTaskHumanReview
      source_table: tasks
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_merge_pending
      constant: EventTaskMergePending
      source_table: tasks
      handler: handleTaskMergePending
      handler_file: cmd/governor/handlers_maint.go

    - event: task_merged
      constant: EventTaskMerged
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_review
      constant: EventTaskReview
      source_table: tasks
      handler: handleTaskReview
      handler_file: cmd/governor/handlers_task.go

    - event: task_review_completed
      constant: EventTaskReviewCompleted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_review_started
      constant: EventTaskReviewStarted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_started
      constant: EventTaskStarted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_test_completed
      constant: EventTaskTestCompleted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_test_started
      constant: EventTaskTestStarted
      handler: none (no router.On registration)
      handler_file: N/A

    - event: task_testing
      constant: EventTaskTesting
      source_table: tasks
      handler: handleTaskTesting
      handler_file: cmd/governor/handlers_testing.go

    - event: test_results
      constant: EventTestResults
      source_table: test_results
      handler: none (no router.On registration)
      handler_file: N/A

  # Dashboard-only tables (SSE feed, no internal handlers)
  sse_only_tables:
    - orchestrator_events
    - models
    - platforms
    - exchange_rates

  # Statistics (auto-calculated)
  stats:
    total_events: 43
    with_handlers: 11
    without_handlers: 32
