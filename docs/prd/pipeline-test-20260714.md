# Pipeline Test — Courier Task

## Overview
A single courier task to verify the PIF pipeline fix is working end-to-end.
This task requires NO codebase access — purely web research.

## Goal
Verify that: webhook → create_plan → planner → supervisor → createTasks → plan_approved all works.

## Tasks
1. Research and summarize 3 new features in PostgreSQL 17 that are relevant for application developers.
   Output: A brief markdown summary saved to knowledgebase/pg17-features.md
   Expected: 3-5 bullet points with brief explanations.
