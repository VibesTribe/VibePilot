# Test: Hello World Function

## Summary
Create a simple Go function that returns "Hello, World!" to verify the full VibePilot flow.

## Requirements
1. Create `pkg/hello/hello.go` with a `Hello() string` function
2. Function should return "Hello, World!"

## Acceptance Criteria
- [ ] `pkg/hello/hello.go` exists
- [ ] `Hello()` function returns "Hello, World!"
