# Hello World

## Overview
Create a simple hello world function in Go to test the VibePilot end-to-end flow.

## Requirements
- Create a package `hello` in `governor/cmd/hello/`
- Function `Hello()` that returns "Hello, VibePilot!"
- Add tests to verify the function returns correct value
