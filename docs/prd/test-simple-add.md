# Test: Simple Math Utility

## Summary
Create a simple Go package with a math utility function to verify the full VibePilot flow works correctly.

## Requirements
1. Create `pkg/math/add.go` with an `Add(a, b int) int` function
2. Create `pkg/math/add_test.go` with basic tests
3. Function should return the sum of two integers

## Acceptance Criteria
- [ ] `pkg/math/add.go` exists with `Add` function
- [ ] `pkg/math/add_test.go` exists with passing tests
- [ ] Tests cover: positive numbers, negative numbers, zero

## Technical Notes
- Standard Go package structure
- Use standard `testing` package
- Simple table-driven tests

## Files to Create
- `pkg/math/add.go`
- `pkg/math/add_test.go`
