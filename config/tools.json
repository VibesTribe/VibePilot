{
  "version": "1.0",
  "description": "All available tools in VibePilot. Tools are primitives that skills use. Agents are scoped to specific tools.",
  "tools": [
    {
      "id": "browser",
      "name": "Web Browser",
      "description": "Navigate websites, fill forms, click, extract content. Used by courier role.",
      "type": "external",
      "current_implementation": "browser-use",
      "implementation_notes": {
        "browser-use": "Model-driven. LLM sees page, decides actions via vision/reasoning. Flexible, model-agnostic.",
        "playwright-cdp": "Deterministic. Snapshot returns refs, model picks ref, Playwright executes. More reliable but requires CDP setup. OpenClaw uses this approach."
      },
      "requires": ["browser-use"],
      "swap_evaluated": "2026-02-17",
      "swap_notes": "browser-use chosen for flexibility. Playwright+CDP (OpenClaw style) is viable alternative. Evaluate monthly as landscape evolves rapidly."
    },
    {
      "id": "file_read",
      "name": "File Read",
      "description": "Read files from filesystem. Used by internal runners for codebase access.",
      "type": "internal"
    },
    {
      "id": "file_write",
      "name": "File Write",
      "description": "Write files to filesystem. Used by internal runners to create/modify code.",
      "type": "internal"
    },
    {
      "id": "supabase_query",
      "name": "Supabase Query",
      "description": "Query Supabase database for state, metrics, task history. Used by Vibes agent.",
      "type": "external",
      "requires": ["supabase_client"]
    },
    {
      "id": "supabase_write",
      "name": "Supabase Write",
      "description": "Write to Supabase database. Used by orchestrator for state updates.",
      "type": "external",
      "requires": ["supabase_client"]
    },
    {
      "id": "web_search",
      "name": "Web Search",
      "description": "Search the web for information. Used by research skill.",
      "type": "external"
    },
    {
      "id": "git",
      "name": "Git Operations",
      "description": "Branch, commit, push, merge. Used by internal runners for code changes.",
      "type": "internal"
    },
    {
      "id": "cli",
      "name": "CLI Execution",
      "description": "Execute shell commands. Used by internal CLI runner.",
      "type": "internal"
    },
    {
      "id": "voice_stt",
      "name": "Speech to Text",
      "description": "Convert speech to text. Used by Vibes for voice input.",
      "type": "external",
      "requires": ["deepgram"]
    },
    {
      "id": "voice_tts",
      "name": "Text to Speech",
      "description": "Convert text to speech. Used by Vibes for voice output.",
      "type": "external",
      "requires": ["kokoro"]
    }
  ]
}
